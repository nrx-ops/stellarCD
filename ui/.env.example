# Kubernetes API URL (for development, typically localhost)
# In production, set to your cluster's API server or reverse proxy URL
VITE_KUBE_API_URL=http://localhost:8080

# API request timeout (milliseconds)
VITE_API_TIMEOUT=30000
