apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
# Uncomment to expose the dashboard through an Ingress. Set a real hostname in
# ingress.yaml first; see the notes at the top of that file.
#- ingress.yaml
